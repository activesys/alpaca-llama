"""
test_table.py

unit testing for table.py
"""

import unittest
from table import StateTransitionTable

class TestStateTransitionTable(unittest.TestCase):
    def setUp(self):
        self.table = StateTransitionTable()

    def test_next_state(self):
        # START -> IN-ESCAPE
        self.assertEqual(self.table.next_state(self.table.STATE_START, '\\'), self.table.STATE_IN_ESCAPE)
        # START -> FINISH
        self.assertEqual(self.table.next_state(self.table.STATE_START, '|'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '*'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '+'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '('), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, ')'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '.'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '^'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '['), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, ']'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '-'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '0'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '1'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '2'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '3'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '4'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '5'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '6'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '7'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '8'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '9'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'a'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'b'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'c'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'd'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'e'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'f'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'g'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'h'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'i'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'j'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'k'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'l'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'm'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'n'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'o'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'p'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'q'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'r'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 's'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 't'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'u'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'v'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'w'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'x'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'y'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'z'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'A'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'B'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'C'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'D'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'E'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'F'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'G'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'H'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'I'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'J'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'K'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'L'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'M'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'N'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'O'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'P'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'Q'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'R'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'S'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'T'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'U'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'V'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'W'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'X'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'Y'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, 'Z'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '!'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '"'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '#'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '$'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '%'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '&'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, "'"), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, ','), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '/'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, ':'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, ';'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '<'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '='), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '>'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '?'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '@'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '_'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '{'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '}'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_START, '~'), self.table.STATE_FINISH)
        # IN-ESCAPE -> FINISH
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '|'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '*'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '+'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '('), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, ')'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '\\'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '.'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '^'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '['), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, ']'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '-'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'a'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'b'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'c'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'f'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'n'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'r'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 't'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'v'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'd'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'D'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'w'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'W'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 's'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'S'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'l'), self.table.STATE_FINISH)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'C'), self.table.STATE_FINISH)
        # IN-ESCAPE -> None
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '0'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '1'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '2'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '3'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '4'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '5'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '6'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '7'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '8'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '9'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'e'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'g'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'h'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'i'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'j'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'k'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'm'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'o'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'p'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'q'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'u'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'x'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'y'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'z'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'A'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'B'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'E'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'F'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'G'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'H'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'I'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'J'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'K'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'L'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'M'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'N'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'O'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'P'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'Q'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'R'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'T'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'U'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'V'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'X'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'Y'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, 'Z'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '!'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '"'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '#'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '$'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '%'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '&'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, "'"), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, ','), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '/'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, ':'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, ';'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '<'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '='), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '>'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '?'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '@'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '_'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '{'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '}'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_IN_ESCAPE, '~'), None)
        # FINISH -> None
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '|'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '*'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '+'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '('), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, ')'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '\\'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '.'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '^'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '['), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, ']'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '-'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '0'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '1'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '2'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '3'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '4'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '5'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '6'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '7'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '8'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '9'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'a'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'b'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'c'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'd'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'e'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'f'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'g'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'h'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'i'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'j'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'k'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'l'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'm'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'n'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'o'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'p'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'q'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'r'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 's'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 't'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'u'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'v'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'w'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'x'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'y'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'z'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'A'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'B'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'C'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'D'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'E'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'F'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'G'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'H'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'I'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'J'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'K'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'L'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'M'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'N'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'O'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'P'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'Q'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'R'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'S'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'T'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'U'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'V'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'W'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'X'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'Y'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, 'Z'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '!'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '"'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '#'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '$'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '%'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '&'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, "'"), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, ','), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '/'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, ':'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, ';'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '<'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '='), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '>'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '?'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '@'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '_'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '{'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '}'), None)
        self.assertEqual(self.table.next_state(self.table.STATE_FINISH, '~'), None)

